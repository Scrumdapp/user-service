operations:
  - add_column:
      table: users
      column:
        name: email
        type: varchar(255)
        nullable: true
        unique: true

  - drop_column:
      table: users
      column: discord_id
      down: "0"
